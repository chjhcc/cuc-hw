# 作业7

## 1. 写出二进制IP地址(11000000 10101000 00001010 11111110)的点分十进制数表示
200.168.34.254

## 2.写出以下地址的二进制形式：
222.31.76.101------------11011110000111110100110001100101
10.64.128.230------------00001010010000001000000000010111

## 3. 简单说明转发器、交换机、路由器和网关有何区别？
### 转发器：
工作在OSI参考模型的物理层，主要功能是放大和整形信号，以便数据能够在相同的网络中继续传输。它不对数据进行任何处理，只是简单地将信号从一个端口转发到另一个端口。

### 交换机：
工作在OSI参考模型的数据链路层，主要用于连接同一局域网内的设备，通过MAC地址来转发数据帧。交换机可以有效地将网络分割成多个冲突域，提高网络效率。

### 路由器：
工作在OSI参考模型的网络层，负责将数据包从一个网络转发到另一个网络。路由器使用IP地址来确定最佳的路径，可以实现不同网络之间的通信，并提供诸如分组过滤、优先级处理、防火墙等功能。

### 网关：
工作在OSI参考模型的传输层及以上，它是连接两个不同网络体系的设备，不仅能够连接不同的网络，还能实现协议转换，使得基于不同体系结构的网络能够相互通信。

## 4. 简单说明下列协议的作用：IP、ARP、RARP、ICMP

### IP协议:
实现网络互连。使参与互连的性能各异的网络从用户看起来好似是一个统一的网络。网际协议IP是TCP/IP体系中两个最主要的协议之一,与IP协议配套使用的还有四个协议。

### ARP协议:
是解决同一个局域网上的主机或路由器的IP地址和硬件地址的映射问题。

### RARP:
是解决同一个局域网上的主机或路由器的硬件地址和IP地址的映射问题。

### ICMP:
提供过失报告和询问报文,以提高IP数据交付成功的时机

## 5. IP地址分为几类？各如何表示？IP地址的主要特点是什么？

### IP地址共分为5类：A,B,C,D,E。

### 表示：
A,B,C三类的网络号字段分别为1个，2个，3个字节；而网络号字段的最前面有1~3位的类别比特，其二进制数值分别为0,01,110。A类，B类，C类地址的主机号字段分别为3,2,1个字节。
D类地址用于多播；前4位包含二进制数值1110。
E类地址用于保留为未来使用，前5位包含二进制数值11110。

### 特点
（1）是一种分等级的地址结构，每一个IP地址由网络号和主机号两部分组成。
（2）IP地址实际上是标识一个主机（或路由器）和一条链路的接口。当一个主机同时连接到两个网络上时，该主机就必须同时具有两个相应的IP地址，而且其网络号必须是不相同的。
（3）用转发器或网桥连接起来的若干个局域网仍为一个网络，这些局域网都具有相同的网络号。
（4）在IP地址中，所有分配到网络号的网络，不论大小，都是平等的。

## 6. 简述IP地址与硬件地址的区别，为什么有了全局唯一的MAC地址，还需要IP地址？
（1）从层次角度看，物理地址是数据链路层和物理层使用的地址（用于传输MAC帧），而IP地址是网络层和以上各层使用的地址（用于传输IP数据报），是一种逻辑地址（称IP地址为逻辑地址是因为IP地址是用软件实现的）。
（2）从各自的角度及使用原因看，IP地址就是给每个连接在因特网上的主机（或路由器）分配一个在全世界范围是唯一的32位的标识符。从而把整个因特网看成为一个单一的、抽象的网络。在实际网络的链路上传送数据帧时，最终还是必须使用硬件地址。MAC地址在一定程度上与硬件一致，基于物理、能够标识具体的链路通信对象，IP地址给予逻辑域的划分、不受硬件限制。

## 7. 试辨认一下IP地址的网络类别：
128.34.253.2          B类
8.8.8.8               A类
222.31.171.54         C类
192.168.10.254        C类
172.16.100.151        B类

## 8. 在电脑上查询ARP缓存列表，截图贴在作业中。根据自己的ARP缓存列表，指出其中每个IP地址是单播、组播还是广播地址？是哪一类的？默认网关的IP地址和MAC地址是什么？
![](./屏幕截图(457).png)
根据这张图片，我们可以进行以下判断：

单播地址：
- 192.168.8.254
- 192.168.8.255
- 192.168.1.1
- 192.168.1.5
- 192.168.1.6
- 192.168.1.7
- 192.168.1.8
- 192.168.1.9
- 192.168.1.11
- 192.168.1.13
- 192.168.1.16
- 192.168.1.21
- 192.168.1.255
- 192.168.6.254
- 192.168.6.255

组播地址：
- 224.0.0.2
- 224.0.0.22
- 224.0.0.251
- 224.0.0.252
- 239.255.255.250

广播地址：
- 255.255.255.255

默认网关的IP地址和MAC地址：
- IP地址是192.168.1.1，MAC地址是00-0C-29-FD-4E-95。
